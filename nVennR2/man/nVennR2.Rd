% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nVennR2-package.R
\name{nVennR2}
\alias{nVennR2}
\title{nVennR2}
\description{
An interface to nVenn2 to create n-dimensional, quasi-proportional Venn
diagrams.
}
\details{
The input for nVennR2 is a list of lists or a text table. The dataset
\code{exampledf} contains an example of a list of lists, where each internal
list has a name for the set and some set elements. The result will be a
Venn diagram where each region is represented by a circle whose area is
approximately proportional to the number of elements in that region. Each
circle has two labels. The number in a larger font size represents the
number of elements in the region. The smaller numbers in parentheses
show which sets the region belongs to.

A diagram is created
with \code{\link[=nVennDiagram]{nVennDiagram()}} and plotted and/or saved with \code{\link[=plotVenn]{plotVenn()}}. The
appearance of the plot can be changed with \code{\link[=setVennOpts]{setVennOpts()}} and
\code{\link[=setVennColors]{setVennColors()}}. If a plot is saved, the resulting svg file can be
loaded again with \code{\link[=readVennSVG]{readVennSVG()}}.
}
\examples{
library(nVennR2)
myv <- nVennDiagram(exampledf, verbose=F)
myv <- setVennOpts(myv, opacity=0.2, fontSize=14)

}
\author{
Victor Quesada, Universidad de Oviedo (Spain).
}
